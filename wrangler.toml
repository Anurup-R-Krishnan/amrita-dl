name = "amritapapers"
pages_build_output_dir = "web"
compatibility_date = "2026-08-18"

# Cloudflare Pages deployment configuration for static SPA frontend
[vars]
ENVIRONMENT = "production"
# Set API_BASE_URL when backend API is hosted on a separate domain (e.g. Render / Custom API Subdomain)
# API_BASE_URL = "https://amrita-papers-api.onrender.com"
